# Documentation Artifacts

This folder contains reference documentation and generated deliverables that are
useful for project handoff but are not required by the Cloud Run runtime.

## Contents

- `architecture/`: architecture diagrams and architecture document.
- `business/`: business requirements document.
- `code/`: code-level handoff document.

The deployment build excludes this folder through `.gcloudignore` and
`.dockerignore`.
